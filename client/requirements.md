## Packages
react-speech-recognition | For speech-to-text functionality
regenerator-runtime | Required polyfill for speech recognition
framer-motion | Smooth animations for voice interface and page transitions
lucide-react | Icon library (already in base but good to be explicit)

## Notes
Voice interface requires microphone permissions.
The backend handles the MedGemma AI model proxy.
Tailwind config should extend font family for 'Plus Jakarta Sans' and 'Outfit'.
